import 'dart:ui';

class ColorRangeModel {
  Color firstColor;
  Color midColor;
  Color lastColor;

  ColorRangeModel({
    required this.firstColor,
    required this.midColor,
    required this.lastColor,
  });
}
